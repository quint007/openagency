{
  "name":    "open-agency",
  "version": "1.0.0",
  "packages": [
    "nodejs_20",
    "pnpm",
    "opentofu",
    "docker",
    "go-task",
    "gh",
    "railway@latest",
    "git@latest"
  ],
  "shell": {
    "init_hook": [
      "if [ -f .githooks/pre-commit ]; then mkdir -p .git/hooks && if [ ! -f .git/hooks/pre-commit ] || ! cmp -s .githooks/pre-commit .git/hooks/pre-commit; then cp .githooks/pre-commit .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit; fi; fi"
    ]
  }
}
