# Infrastructure Modules

Reusable OpenTofu module contracts live here.

- `railway/` defines the backend runtime contract and service-facing environment inputs.
- `cloudflare/` defines the DNS contract for environment hostnames.
- `r2/` defines the storage bucket contract and public media hostname shape.
